# Loop Control Statemetns = Changes a loop execution from its normal sequence

# break = used to terminate the loop entirely
# continue = skips to the nnext iteration of the loop
# pass = does nothing acts a placeholder

while True:
    name = input("Enter you name:")
    if name != "":
        break

